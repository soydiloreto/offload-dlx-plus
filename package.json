{
  "name": "offload-dlx-plus-dev",
  "private": true,
  "description": "Local development environment scripts for the Offload+ WordPress plugin. Not published to npm — only used to pin @wordpress/env for contributors.",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/soydiloreto/offload-dlx-plus.git"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:reset": "wp-env destroy && wp-env start",
    "env:cli": "wp-env run cli",
    "env:logs": "wp-env logs"
  },
  "devDependencies": {
    "@wordpress/env": "^11.14.0"
  }
}
